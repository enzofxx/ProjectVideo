<nav class="navbar navbar-expand-lg  navbar-light bg-light">
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <div class="d-flex flex-column">
                <li class="nav-item active">
                    <a class="nav-link" data-toggle="collapse" data-target="#coursesCollapse" aria-expanded="false"
                       aria-controls="coursesCollapse" href="{{ route('course.index') }}">Video pamokos <span
                                class="sr-only">(current)</span></a>
                    <div class="collapse" id="coursesCollapse">
                        <ul>
                            {% for course in courses %}
                                {% if course.purchased %}
                                    <li class="nav-item {% if course.id == id %}active{% endif %}">
                                        <a class="nav-link" href="{{ route('course.show',{'course':course.id}) }}">
                                            <i class="fas fa-unlock fa-2x"></i> {{ course.id }}. {{ course.name }}</a>
                                    </li>
                                {% else %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="">
                                            <i class="fas fa-lock fa-2x"></i> {{ course.id }}. {{ course.name }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ route('user.profile') }}">Mano profilis</a>
                </li>
            </div>
        </ul>
    </div>

</nav>
